/*
 * LoginLink Messages
 *
 * This contains all the text for the LoginLink component.
 */
import { defineMessages } from 'react-intl';

export default defineMessages({
  linkText: {
    id: 'app.components.LoginLink.linkText',
    defaultMessage: 'Log in',
  },
});
